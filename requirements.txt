# torch==2.6.0 and torchvision==0.21.0 (both +cu124) come from the base image
# (pytorch/pytorch:2.6.0-cuda12.4-cudnn9-runtime) -- do not reinstall them here,
# pip would otherwise silently swap in a CPU-only or mismatched-CUDA wheel.
timm==1.0.27
pandas==3.0.3
numpy==2.4.6
pillow==12.2.0
scikit-learn==1.9.0
